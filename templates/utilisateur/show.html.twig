{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
<div class="container mt-5 mb-5" id="form">
    <h1 class="text-center mt-3">Utilisateur</h1>

    <table class="table mt-5 mb-5">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.username }}</td>
            </tr>
            <tr>
                <th>Rôle</th>
                <td>{{ utilisateur.role.label }}</td>
            </tr>
        </tbody>
    </table>
    <div class="row  justify-content-center mb-5">
        <a href="{{ path('utilisateur_index') }}" class="btn btn-warning">retour</a>
        <a href="{{ path('utilisateur_edit', {'id': utilisateur.id}) }}" class="btn btn-success">editer</a>
        {{ include('utilisateur/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
