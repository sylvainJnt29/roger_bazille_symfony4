{% extends 'base.html.twig' %}

{% block title %}Création d'un utilisateur admin'{% endblock %}

{% block body %}

<div class="container">
    <div id="form" class="mt-5 text text-center">
        {{form_start(form)}}
            {{form_widget(form)}}
                <a href="{{path('creationAdmin')}}" class="btn btn-primary">Retour</a>
                <input type="submit" value="valider" class="btn btn-success">
        {{form_end(form)}}
    </div>
</div>
{% endblock %}
