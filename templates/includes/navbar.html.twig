<nav class="navbar navbar-expand-lg  navbar-light bg-info" id="navbar">
  <a class="navbar-brand" href="{{ path('home') }}">Ecole Roger Bazille</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto ">
      <li class="nav-item active">
      
        <a class="nav-link" href="{{ path('home') }}">
            <i class="fas fa-home"></i>
            Accueil 
        </a>
      </li>
      <!--
      <li class="nav-item">
        <a class="nav-link" href="#">Link</a>
      </li>
      -->
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="{{path('ecole')}}"  id="navbarDropdown" role="link" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Ecole
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        <a class="dropdown-item" href="{{path('ecole')}}">Actualités des professeurs</a>
        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_USER_PROF')%}
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="{{path('admin_ecole')}}">Espace professeurs</a>
          {% endif %}
        </div>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Périscolaire
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="{{path('cantine')}}">Cantine</a>
          <a class="dropdown-item" href="{{path('periscolaire')}}">Accueil Périscolaire</a>
          <a class="dropdown-item" href="{{path('tap')}}">T.A.P</a>
        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_USER_MAIRIE')%}
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="{{path('choix_admin')}}">Espace mairie</a>
        </div>
        {% endif %}

      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Parents d'élèves
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="parents_eleves">Actualités des parents d'élèves</a>
          {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_USER_PARENT')%}
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="{{path('admin_parents')}}">Espace parents d'élèves</a>
        </div>
        {% endif %}
      </li>

        <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Conseil d'école
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="{{path('conseil_ecole')}}">Actualités du conseil d'école</a>
          <a class="dropdown-item" href="{{path('questions_reponses')}}">Vos questions</a>
          {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_USER_CONSEIL')%}
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="{{path('choix_admin_conseil')}}">Espace délégués des parents d'élèves</a>
        </div>
        {% endif %}
        
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{path('infos_pratiques')}}">Infos pratiques</a>
      </li>
      </ul>
      
      <ul class="navbar-nav ml-auto">
        {% if not app.user %}
        <li class="nav-item">
        <a class="nav-link" href="{{path('app_login')}}">
          Se connecter
        </a>
        </li>
        {% else %}
        <li class="nav-item">
        <a class="nav-link" href="{{path('app_logout')}}">
          Se déconnecter
        </a>
        </li>
        {% endif %}
      </ul>
      
  </div>
</nav>